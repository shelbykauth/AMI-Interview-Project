import { SkeletonLoader } from './skeleton-loader';

describe('SkeletonLoader', () => {
  it('should create an instance', () => {
    const directive = new SkeletonLoader();
    expect(directive).toBeTruthy();
  });
});
