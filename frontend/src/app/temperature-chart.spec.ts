import { TemperatureChartDirective } from './temperature-chart';

describe('TemperatureChart', () => {
  it('should create an instance', () => {
    const directive = new TemperatureChartDirective();
    expect(directive).toBeTruthy();
  });
});
